server:
  port: 8080

services:
  user: http://localhost:8081
  monolith: http://localhost:8082

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: ${services.user}
          predicates:
            - Path=/users,/users/**,/user,/user/**
        - id: monolith
          uri: ${services.monolith}
          predicates:
            - Path=/articles,/articles/**,/profiles/**,/tags,/tags/**,/graphql,/graphiql,/subscriptions/**
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              - HEAD
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowedHeaders:
              - Authorization
              - Cache-Control
              - Content-Type

logging:
  level:
    org.springframework.cloud.gateway: INFO
