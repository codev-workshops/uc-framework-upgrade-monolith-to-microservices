<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.userservice.infrastructure.UserMapper">
    <insert id="insert">
        insert into users(id, username, email, password, bio, image)
        values(
        #{user.id},
        #{user.username},
        #{user.email},
        #{user.password},
        #{user.bio},
        #{user.image})
    </insert>
    <update id="update">
        update users
        set email = #{user.email},
        username = #{user.username},
        password = #{user.password},
        bio = #{user.bio},
        image = #{user.image}
        where id = #{user.id}
    </update>
    <select id="findById" resultType="io.spring.userservice.domain.User">
        select id, username, email, password, bio, image from users where id = #{id}
    </select>
    <select id="findByUsername" resultType="io.spring.userservice.domain.User">
        select id, username, email, password, bio, image from users where username = #{username}
    </select>
    <select id="findByEmail" resultType="io.spring.userservice.domain.User">
        select id, username, email, password, bio, image from users where email = #{email}
    </select>
    <insert id="saveRelation">
        insert into follows(user_id, follow_id)
        values(#{followRelation.userId}, #{followRelation.targetId})
    </insert>
    <select id="findRelation" resultType="io.spring.userservice.domain.FollowRelation">
        select user_id userId, follow_id targetId from follows
        where user_id = #{userId} and follow_id = #{targetId}
    </select>
    <delete id="deleteRelation">
        delete from follows where user_id = #{followRelation.userId} and follow_id = #{followRelation.targetId}
    </delete>
</mapper>
