<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.commentservice.infrastructure.CommentMapper">
    <insert id="insert">
        insert into comments(id, body, article_id, user_id, created_at, updated_at)
        values(#{comment.id}, #{comment.body}, #{comment.articleId},
               #{comment.userId}, #{comment.createdAt}, #{comment.createdAt})
    </insert>
    <select id="findById" resultType="io.spring.commentservice.domain.Comment">
        select id, body, article_id articleId, user_id userId, created_at createdAt
        from comments where article_id = #{articleId} and id = #{id}
    </select>
    <delete id="delete">
        delete from comments where id = #{id}
    </delete>
</mapper>
