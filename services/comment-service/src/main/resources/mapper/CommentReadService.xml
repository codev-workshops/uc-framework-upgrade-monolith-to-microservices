<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.infrastructure.mybatis.readservice.CommentReadService">
    <!--
      The join to `users` has been REMOVED: comment-service owns only `comments`.
      The author projection (username/bio/image) is resolved out-of-band via
      user-auth-service. Only the author id (C.user_id) is selected here so the
      read model can be enriched afterwards.
    -->
    <sql id="selectCommentData">
        SELECT
        C.id commentId,
        C.body commentBody,
        C.created_at commentCreatedAt,
        C.article_id commentArticleId,
        C.user_id userId
        from comments C
    </sql>

    <select id="findById" resultMap="transfer.data.commentData">
        <include refid="selectCommentData"/>
        where C.id = #{id}
    </select>
    <select id="findByArticleId" resultMap="transfer.data.commentData">
        <include refid="selectCommentData"/>
        where C.article_id = #{articleId}
        order by C.created_at asc
    </select>
</mapper>
