<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.infrastructure.mybatis.readservice.CommentReadService">
    <!--
      comment-service owns only the comments table: the author join against users/follows in the
      monolith is replaced by API composition (user-service + profile-service) in CommentQueryService.
    -->
    <sql id="selectCommentData">
        SELECT
        C.id commentId,
        C.body commentBody,
        C.created_at commentCreatedAt,
        C.updated_at commentUpdatedAt,
        C.article_id commentArticleId,
        C.user_id commentUserId
        from comments C
    </sql>

    <select id="findById" resultMap="commentData">
        <include refid="selectCommentData"/>
        where C.id = #{id}
    </select>

    <select id="findByArticleId" resultMap="commentData">
        <include refid="selectCommentData"/>
        where C.article_id = #{articleId}
        order by C.created_at asc
    </select>

    <select id="countByArticleId" resultType="int">
        select count(1) from comments where article_id = #{articleId}
    </select>

    <resultMap id="commentData" type="io.spring.application.data.CommentData">
        <id column="commentId" property="id"/>
        <result column="commentBody" property="body"/>
        <result column="commentArticleId" property="articleId"/>
        <result column="commentUserId" property="userId"/>
        <result column="commentCreatedAt" property="createdAt"/>
        <result column="commentUpdatedAt" property="updatedAt"/>
    </resultMap>
</mapper>
