<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.infrastructure.mybatis.readservice.TagReadService">
    <select id="all" resultType="string">
        select name from tags
    </select>

    <select id="tagsOfArticle" resultType="string">
        select t.name from tags t
        join article_tags at on at.tag_id = t.id
        where at.article_id = #{articleId}
    </select>

    <select id="tagsOfArticles" resultType="io.spring.application.data.ArticleTagData">
        select at.article_id as articleId, t.name as tagName from tags t
        join article_tags at on at.tag_id = t.id
        where at.article_id in
        <foreach item="articleId" collection="articleIds" open="(" separator="," close=")">
            #{articleId}
        </foreach>
    </select>
</mapper>
