<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.favoriteservice.infrastructure.mybatis.readservice.ArticleFavoritesReadService">
    <select id="isUserFavorite" resultType="java.lang.Boolean">
        select count(1) from article_favorites where user_id = #{userId} and article_id = #{articleId}
    </select>
    <select id="articleFavoriteCount" resultType="java.lang.Integer">
        select count(1) from article_favorites where article_id = #{articleId}
    </select>
    <select id="articlesFavoriteCount" resultMap="favoriteCount">
        select article_id as id, count(user_id) as favoriteCount from article_favorites
        where article_id in
        <foreach collection="ids" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        group by article_id
    </select>
    <select id="userFavorites" resultType="java.lang.String">
        select article_id
        from article_favorites
        where article_id in
        <foreach collection="ids" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        and user_id = #{userId}
    </select>
    <resultMap id="favoriteCount" type="io.spring.favoriteservice.infrastructure.mybatis.readservice.ArticleFavoriteCountResult">
        <result column="id" property="id"/>
        <result column="favoriteCount" property="favoriteCount"/>
    </resultMap>
</mapper>
