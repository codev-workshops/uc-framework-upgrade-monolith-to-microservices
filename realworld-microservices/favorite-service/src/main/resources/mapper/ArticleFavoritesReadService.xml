<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.favorite.infrastructure.ArticleFavoritesReadService">
    <select id="isUserFavorite" resultType="java.lang.Boolean">
        select count(1) from article_favorites where user_id = #{userId} and article_id = #{articleId}
    </select>
    <select id="articleFavoriteCount" resultType="java.lang.Integer">
        select count(1) from article_favorites where article_id = #{articleId}
    </select>
    <select id="articlesFavoriteCount" resultMap="transfer.data.favoriteCount">
        select AF.article_id as id, count(AF.user_id) as favoriteCount from article_favorites AF
        where AF.article_id in
        <foreach collection="ids" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        group by AF.article_id
    </select>
    <select id="userFavorites" resultType="java.lang.String">
        select
          AF.article_id
        from article_favorites AF
        where AF.article_id in
        <foreach collection="ids" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        and AF.user_id = #{userId}
    </select>
    <resultMap id="transfer.data.favoriteCount" type="io.spring.shared.dto.ArticleFavoriteCount">
        <result column="id" property="id"/>
        <result column="favoriteCount" property="count"/>
    </resultMap>
</mapper>
