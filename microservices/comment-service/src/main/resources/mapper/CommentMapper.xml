<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.commentservice.infrastructure.mybatis.mapper.CommentMapper">
    <insert id="insert">
        insert into comments(id, body, article_id, user_id, created_at, updated_at)
        values(
          #{comment.id},
          #{comment.body},
          #{comment.articleId},
          #{comment.userId},
          #{comment.createdAt},
          #{comment.updatedAt}
        )
    </insert>
    <delete id="delete">
        delete from comments where id = #{id}
    </delete>
    <select id="findById" resultMap="comment">
        select id, body, article_id, user_id, created_at, updated_at
        from comments
        where article_id = #{articleId} and id = #{id}
    </select>

    <resultMap id="comment" type="io.spring.commentservice.core.comment.Comment">
        <id column="id" property="id"/>
        <result column="body" property="body"/>
        <result column="article_id" property="articleId"/>
        <result column="user_id" property="userId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
</mapper>
