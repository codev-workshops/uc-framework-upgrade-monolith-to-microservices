<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.commentservice.infrastructure.mybatis.readservice.CommentReadService">
    <select id="findById" resultType="io.spring.commentservice.application.data.CommentData">
        select id, body, article_id articleId, user_id userId, created_at createdAt, updated_at updatedAt
        from comments where id = #{id}
    </select>
    <select id="findByArticleId" resultType="io.spring.commentservice.application.data.CommentData">
        select id, body, article_id articleId, user_id userId, created_at createdAt, updated_at updatedAt
        from comments where article_id = #{articleId}
        order by created_at desc
    </select>
</mapper>
